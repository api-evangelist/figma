naftiko: 1.0.0-alpha2
info:
  label: Figma Dev Resources API — Dev Resources
  description: 'Figma Dev Resources API — Dev Resources. 2 operations. Lead operation: Figma Create Dev Resources. Self-contained
    Naftiko capability covering one Figma business surface.'
  tags:
  - Figma
  - Dev Resources
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    FIGMA_API_KEY: FIGMA_API_KEY
capability:
  consumes:
  - type: http
    namespace: dev-resources-dev-resources
    baseUri: https://api.figma.com
    description: Figma Dev Resources API — Dev Resources business capability. Self-contained, no shared references.
    resources:
    - name: v1-dev_resources
      path: /v1/dev_resources
      operations:
      - name: postdevresources
        method: POST
        description: Figma Create Dev Resources
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
      - name: putdevresources
        method: PUT
        description: Figma Update Dev Resources
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: bearer
      token: '{{env.FIGMA_API_KEY}}'
  exposes:
  - type: rest
    namespace: dev-resources-dev-resources-rest
    port: 8080
    description: REST adapter for Figma Dev Resources API — Dev Resources. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/v1/dev-resources
      name: v1-dev-resources
      description: REST surface for v1-dev_resources.
      operations:
      - method: POST
        name: postdevresources
        description: Figma Create Dev Resources
        call: dev-resources-dev-resources.postdevresources
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: PUT
        name: putdevresources
        description: Figma Update Dev Resources
        call: dev-resources-dev-resources.putdevresources
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: dev-resources-dev-resources-mcp
    port: 9090
    transport: http
    description: MCP adapter for Figma Dev Resources API — Dev Resources. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: figma-create-dev-resources
      description: Figma Create Dev Resources
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: dev-resources-dev-resources.postdevresources
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: figma-update-dev-resources
      description: Figma Update Dev Resources
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: dev-resources-dev-resources.putdevresources
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
