naftiko: 1.0.0-alpha2
info:
  label: Figma Images API — Images
  description: 'Figma Images API — Images. 1 operations. Lead operation: Figma Render Images of File Nodes. Self-contained
    Naftiko capability covering one Figma business surface.'
  tags:
  - Figma
  - Images
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    FIGMA_API_KEY: FIGMA_API_KEY
capability:
  consumes:
  - type: http
    namespace: images-images
    baseUri: https://api.figma.com
    description: Figma Images API — Images business capability. Self-contained, no shared references.
    resources:
    - name: v1-images-file_key
      path: /v1/images/{file_key}
      operations:
      - name: getimages
        method: GET
        description: Figma Render Images of File Nodes
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.FIGMA_API_KEY}}'
  exposes:
  - type: rest
    namespace: images-images-rest
    port: 8080
    description: REST adapter for Figma Images API — Images. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/v1/images/{file-key}
      name: v1-images-file-key
      description: REST surface for v1-images-file_key.
      operations:
      - method: GET
        name: getimages
        description: Figma Render Images of File Nodes
        call: images-images.getimages
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: images-images-mcp
    port: 9090
    transport: http
    description: MCP adapter for Figma Images API — Images. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: figma-render-images-file-nodes
      description: Figma Render Images of File Nodes
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: images-images.getimages
      outputParameters:
      - type: object
        mapping: $.
